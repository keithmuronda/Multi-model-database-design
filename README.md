# Multi-model-database-design
One library domain modelled three ways — Oracle relational with constraints, views and a PL/SQL procedure; MongoDB documents with aggregation pipelines and $lookup; and a Neo4j graph. Same data, three shapes, compared.
